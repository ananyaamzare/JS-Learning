


var city; // variable declaration
let street;
const pin = 57859;
city = "pune";
street = "mardi road";
// const pin = "6786897"; not allow

var city; // var re declaration 
//let street; //re declaration are not allow

function display(){
    console.log((`inside the function `));
    let isMarried = true;
    if (isMarried) {
        var greet = ("good morning");
        const PI = 3.1412;
        console.log();
    }
    // console.log(PI);
    // console.log(greet);
}

